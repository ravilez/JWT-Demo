apiVersion: v1
kind: Service
metadata:
  name: jwt-demo
  namespace: jwt-demo
spec:
  selector:
    app: jwt-demo
  ports:
    - name: http
      protocol: TCP
      port: 80
      targetPort: 8080
  type: ClusterIP
