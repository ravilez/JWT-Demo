
apiVersion: v1
kind: Service
metadata:
  name: remote-cnccontroller
spec:
  type: NodePort
  selector:
    app: remote-cnccontroller
  ports:
    - port: 80
      targetPort: 8080
      nodePort: 32000
